server:
  port: 80


spring:
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
      path: /h2-console
#  jpa:
#    hibernate:
#      ddl-auto: create-drop
#    show-sql: true
#    generate-ddl: true
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb

token:
  expiration_time: 86400000   # 24hour
  secret: user_token